apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config
  namespace: plex
spec:
  storageClassName: longhorn-ssd-fast
  volumeName: pv-plex-config
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 12G

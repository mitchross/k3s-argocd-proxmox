apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: 1passwordconnect

resources:
  - namespace.yaml
  - http-route.yaml

helmCharts:
  - name: connect
    repo: https://1password.github.io/connect-helm-charts
    version: 1.15.1  # Check for the latest version
    releaseName: "connect"
    namespace: 1passwordconnect
    valuesFile: values.yaml
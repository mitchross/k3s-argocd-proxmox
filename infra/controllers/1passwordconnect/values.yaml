connect:
  credentials:
    existingSecret: 1password-credentials
    existingSecretKey: 1password-credentials.json

operator:
  create: true
  token:
    existingSecret: 1password-operator-token
    existingSecretKey: token

# Optional resource limits if you want them
  api:
    enabled: true
    resources:
      requests:
        memory: "150Mi"
        cpu: "100m"
      limits:
        memory: "300Mi"
        cpu: "200m"

  sync:
    enabled: true
    resources:
      requests:
        memory: "150Mi"
        cpu: "100m"
      limits:
        memory: "300Mi"
        cpu: "200m"
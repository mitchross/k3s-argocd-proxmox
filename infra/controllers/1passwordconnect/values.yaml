connect:
  credentials:
    existingSecret: 1password-credentials
    existingSecretKey: 1password-credentials.json  # Make sure this matches the key in your secret

operator:
  create: true
  token:
    existingSecret: 1password-operator-token
    existingSecretKey: token
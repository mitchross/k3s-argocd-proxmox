connect:
  credentials: 
    existingSecret: 1passwordconnect
    secretKey: 1password-credentials.json
  serviceType: ClusterIP

operator:
  create: true
  token:
    existingSecret: 1passwordconnect
    existingSecretKey: token
apiVersion: v1
kind: Service
metadata:
  name: longhorn-frontend-service
  namespace: comfyui
  labels:
    app: longhorn-frontend
spec:
  selector:
    app: longhorn-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
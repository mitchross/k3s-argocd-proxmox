# values.yaml
storageClass:
  hostpath:
    enabled: false
    isDefaultClass: false

localprovisioner:
  enabled: true
  # This is the crucial part we were missing
  basePath: "/datapool/kubernetes"
  deviceClass:
    enabled: false
    isDefaultClass: false

zfs-localpv:
  enabled: false

csi:
  zfs:
    enabled: false
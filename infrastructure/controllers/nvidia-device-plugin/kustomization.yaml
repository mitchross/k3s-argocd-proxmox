apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gpu-device-plugin
metadata:
  name: nvidia-device-plugin
  labels:
    app.kubernetes.io/name: nvidia-device-plugin
    app.kubernetes.io/component: device-plugin
resources:
- namespace.yaml
- runtime.yaml
- rbac.yaml
- config.yaml
- service.yaml
# The NVIDIA device plugin DaemonSet is managed by the GPU Operator.
# Remove it here to avoid conflicts and duplicate registrations.
# - nvidia-device-plugin.yml
#- nvidia-powerlimit-daemonset.yaml
# Test pods (uncomment to deploy)
# - nvidia-test-pod.yaml
commonLabels:
  app.kubernetes.io/part-of: gpu-infrastructure

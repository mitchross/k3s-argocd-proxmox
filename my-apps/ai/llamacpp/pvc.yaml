apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llamacpp-storage-pvc
  namespace: llamacpp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi
  storageClassName: ollama-smb
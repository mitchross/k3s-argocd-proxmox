apiVersion: v1
kind: Service
metadata:
  name: llamacpp
  namespace: llamacpp
  labels:
    app: llamacpp
spec:
  selector:
    app: llamacpp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
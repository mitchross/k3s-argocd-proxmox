apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-storage
spec:
  storageClassName: openebs-mayastor-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

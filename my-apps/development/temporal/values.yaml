server:
  replicaCount: 1
  config:
    persistence:
      visibilityStore: default # Use default (Cassandra) store for visibility instead of Elasticsearch
cassandra:
  config:
    cluster_size: 1
elasticsearch:
  enabled: false # Disable Elasticsearch completely
prometheus:
  enabled: false
grafana:
  enabled: false

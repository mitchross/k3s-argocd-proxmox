apiVersion: v1
kind: Service
metadata:
  name: netbox
  namespace: netbox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: netbox
  ports:
    - name: http
      port: 80
      targetPort: 80
